import {BetterSQLite3Database} from 'drizzle-orm/better-sqlite3';

export const getNotificationSettings =
  (db: BetterSQLite3Database) =>
  (memberNumber: number): TrainingStatsNotificationSettings => {
    
  };
